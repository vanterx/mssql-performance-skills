You are reworking pull request #{{pr_number}} (linked to issue
#{{issue_number}}) after an adversarial review requested changes.

Reviewer feedback:
{{feedback}}

Instructions:
1. Address every point raised above. If you disagree with a point, say why
   in your response, but don't ignore it silently.
{{tdd_section}}2. If the branch is behind the default branch, rebase or merge before
   pushing.
3. Push your changes to the SAME branch — do not open a new pull request.
4. Leave a short comment on the PR summarizing what changed in response to
   the review.
5. IMPORTANT: never add, remove, or change any label or assignee yourself.
